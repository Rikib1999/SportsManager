<UserControl x:Class="CSharpZapoctak.Views.CompetitionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CSharpZapoctak.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" Margin="20">
        <Button Content="Edit" Command="{Binding NavigateAddEditCompetitionCommand}" HorizontalAlignment="Right" Width="60"/>
        <Image Source="{Binding CurrentCompetition.LogoPath}" MaxHeight="250" MaxWidth="300" MinHeight="300" MinWidth="150"/>
        <TextBlock FontSize="30" TextWrapping="Wrap" Text="{Binding CurrentCompetition.Name}" TextAlignment="Center" Margin="20"/>
        <TextBlock FontSize="20" TextWrapping="Wrap" Text="{Binding CurrentCompetition.Info}" TextAlignment="Left"/>
    </StackPanel>
</UserControl>
