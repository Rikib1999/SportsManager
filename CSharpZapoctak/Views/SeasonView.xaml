<UserControl x:Class="CSharpZapoctak.Views.SeasonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CSharpZapoctak.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" Margin="20">
            <Button Content="Edit" Command="{Binding NavigateEditSeasonCommand}" HorizontalAlignment="Right" Width="60"/>
            <Image Source="{Binding CurrentSeason.LogoPath}" MaxHeight="500" MaxWidth="500" MinHeight="300" MinWidth="150"/>
            <TextBlock FontSize="30" TextWrapping="Wrap" Text="{Binding CurrentSeason.Name}" TextAlignment="Center" Margin="20"/>
            <TextBlock FontSize="20" TextWrapping="Wrap" Text="{Binding CurrentSeason.Info}" TextAlignment="Left"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>